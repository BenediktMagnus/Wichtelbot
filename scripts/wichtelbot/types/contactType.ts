enum ContactType
{
    Contact = 'contact', // After first contact
    Member = 'member', // After registration
    Wichtel = 'wichtel', // After the assignment has happened
}

export default ContactType;
